It's easy because of the constraints, otherwise need to parse into an ast which is non-trivial.

6/19\
There's some trick of doing it one pass iteratively, however it has its limit.\
Not a great question.
